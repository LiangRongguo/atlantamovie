spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/AtlantaMovie?useSSL=true
    username: root
    password: root2020!
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

logging:
  level:
    root: info
    com.lrg: debug
  file:
    name: log/demo-dev.log
